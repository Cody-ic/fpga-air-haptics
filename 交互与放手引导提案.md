# 触见：交互与放手引导提案

更新日期：2026-09-24

本文汇总团队讨论的使用流程、上位机交互和放手引导方案，与[设计思路](设计思路.md)配套。腕托与语音引导已确定为首版拟采用的方向；结构尺寸、播放载体与实际效果仍需验证。手掌示意背景已在上位机实现，语音、腕托和手部跟踪尚未实现。

## 1. 使用者如何找到放手位置

屏幕上的手掌不能承担视障使用者的定位引导。首版拟采用**摸得到的腕托或定位凹槽，加上操作语音**，帮助使用者把手掌放到固定体验区域。

- 腕托位于阵列侧边，手腕接触支撑，手掌伸到阵列上方；结构避免遮挡发声区域。
- 支撑高度可调，先通过实验确定工作高度、手掌伸入距离与姿态，不把此前讨论的 15～20 cm 固定为产品参数。
- 形状和尺寸需考虑不同手型、左右手和放置舒适度，不能假设放上手腕就一定完全对准。
- 按键保留可触摸的区分方式，例如形状、间距或凸点；停止键应容易找到。具体结构尚未定型。

首轮验证以下使用流程：

1. 使用者摸到定位凹槽，听到“请将手腕放入凹槽，手掌朝下，手指自然伸直”。
2. 通过设备按键选择基础图形，语音播报所选名称。
3. 使用者主动按下播放键；设备进入运行状态后，播报“开始播放”。
4. 按暂停键关闭刺激并保留播放进度；按停止键回到待机。

无位置检测时，不播报“手掌已到位”或自动开始，也不显示位置检测成功。联机语音若依赖电脑，必须与未来设备独立语音功能区别说明。语音模块、扬声器、音量控制与本地触发方式在硬件选型时确定。

## 2. 放手引导与手部跟踪分别解决什么

腕托和操作语音解决“使用者该把手放哪里”；传感器解决“设备如何知道手实际在哪里”。首版先围绕固定位置验证触感与图形辨识，不把自由移动时的跟随作为首轮前提。

后续若加入手部检测，应先明确需要检测的是有无手、高度，还是完整位置与姿态，再选择传感器。选型与标定完成后，才增加有数据依据的“抬高一点”“向左一点”等纠正提示。

跟随模式拟要求有效的检测结果和坐标标定；手部丢失、数据过期或超出已验证范围时暂停输出。此处为后续要求，当前协议和软件没有手部坐标或跟踪置信度。不能用焦点回读代替手掌检测，也不能声称当前具备移手自动停止功能。

## 3. 上位机与设备的操作流程

上位机已提供 Demo 和真实串口客户端。Demo 模拟板端接收命令并通过同一帧协议返回状态；真实连接仍需 FPGA 实现配套固件，不能把上位机写好等同于整机已完成。

| 阶段 | 当前软件行为 |
|---|---|
| 连接 | 选择 Demo 或真实串口；真实连接前设置端口和波特率，连接后锁定，断开后可改 |
| 选图 | 基本图案按钮与任意坐标自定义绘图均可使用，待发送内容与设备内容分开 |
| 文件 | 保存和导入 JSON，显示文件名与未保存状态；替换草稿前提示，导入不自动发送或播放 |
| 发送 | 一次提交完整图形与参数；等待应答及其后内容、版本匹配的完整状态 |
| 播放 | 发送确认前按钮置灰；连接成功或只有应答均不会启用播放 |
| 反馈 | 普通界面显示已接收轮廓、运行状态和回传位置；暂停保留位置，停止隐藏活动亮点 |
| 改图与重连 | 修改后的图形须重新发送；新连接须重新提交确认，不自动播放 |
| 失联 | 回传过期后不再展示活动位置，并禁用播放；实际停机仍依赖板端实现 |

普通模式保留使用者需要的操作和连接参数，包括波特率；相位、理论声场、报文等放在调试模式。具体字段和异常行为以 [HAP3 协议](desktop_app/PROTOCOL.md)为准。

## 4. 图形、阵列与扫描方式

- 预设包括固定点、水平线、竖直线、圆、正方形、三角形和箭头。
- 自定义图形统一为组合草图：连续直线、矩形、圆、圆弧和控制点曲线，允许擦除、关系与尺寸、整体缩放。原打点操作作为连续直线工具保留，不再单列模式。
- 编译路径最多 32 段、合计 256 个坐标，与换能器数量无对应关系。4×4 阵列不等于只能选择 16 个位置；软件容量不代表触觉辨识能力。旧 64 点文件仍可导入。
- 轮廓允许手动排序或跳过；公共线按最终顺序只输出一次，尽量避免闭合轮廓共边。新草图沿显式路径前进，段间关闭输出再切换，不自动连起断开的线。采用单焦点扫描，多焦点属于扩展。
- 电脑提交完整图形；硬件计划由 FPGA 自主完成轨迹插值、相位求解与高速更新，不依赖电脑连续逐点发令。
- 更换阵列保留图形坐标，并使用实际安装的阵元。大阵列可以尝试呈现小阵列使用的设计，前提是坐标范围和协议容量兼容；反向也按实际范围检查，不按节点数直接拒绝，不静默裁剪或缩放。

硬件按 4×4 起步、8×8 扩展，16×16 为后续方向。Demo 的两秒一圈用于观察流程；高速扫描的触觉效果、图形辨识率和手部移动影响尚未验证。

## 5. 手掌示意可视化

已在图案预览和播放画面使用手掌轮廓作背景，叠加完整图形与回传位置，使演示者更容易理解触觉呈现的预期位置。轮廓使用固定参考坐标，不是实测手型；图形和手掌共同缩放，偏移或超出掌心的图形保持原有相对位置。

- 普通界面标注“手掌示意”，不标注“检测到手掌”或“已对准”。
- 图形仍使用既定物理坐标，手掌背景不改变发送内容，不把所有图形自动拉伸到掌心。
- 移动亮点继续来自 Demo 或真实设备的状态回传，不能用本地动画冒充真实反馈。
- 暂停、停止、失联的显示规则保持不变；手掌轮廓本身不表示设备正在输出。
- 日后接入真实手部测量，再区分参考轮廓与实测位置，并完成传感器到阵列的坐标标定。

该背景用于演示和辅助理解；视障使用者的放手引导仍由实体定位与语音承担。手掌示意不表示已经实现放手检测，也不限定或保证触觉有效范围。

## 6. 实现与验证分工

三人分别主责 FPGA 与算法、硬件与阵列、上位机与系统集成，各自验证模块，整机验证共同完成，不设置只负责测试的岗位。

- 硬件与阵列：先制作可调整的定位样件，检查放手姿态、支撑舒适度及对声场的影响，再定版。
- FPGA 与算法：实现本地按键、轨迹和状态回传，验证播放、暂停、停止与模式仲裁。
- 上位机与集成：维护手掌示意与回读一致性，梳理语音触发时机并接入选定的播放载体。
- 共同记录：首次找到腕托和完成放手的时间、所需协助次数、重复放置偏差、语音理解情况、图形辨识结果。目标值根据首轮实验确定。

放手是否方便与图形是否能辨认应分别评价；团队内部体验不能替代视障使用者验证。本文记录设计决策，不新增器件性能或实测结论；技术背景与参考资料见[设计思路第 9 节](设计思路.md#9-参考资料与核验范围)。
