# 上位机联调与验证计划

更新日期：2026-09-25。本文面向上位机负责人及 FPGA 对接同学，整理板卡到货前可完成的工作、首轮联调要求和记录模板。协议细节以 [HAP3 串口协议](desktop_app/PROTOCOL.md)为准。

## 1. 当前基础与近期目标

上位机已有绘图、保存导入、手掌预览、Demo、真实串口接口和 Windows 便携 EXE。**当前 FPGA 开发板和驱动 PCB 都没有到位，没有可连接的实体设备。** 仓库尚无 RTL、PCB 工程或硬件实测结果。近期只开展离线软件验证与交接准备；蓝牙与手机端暂缓。

| 阶段 | 上位机负责人可做的事 | 完成依据 |
|---|---|---|
| 现在：无 FPGA 板、无 PCB | EXE 异机试用、保存与导入测试图形、用 Demo 核对通信流程、与队友约定接口 | 软件测试报告、JSON 文件、协议样例与问题记录 |
| FPGA 板到位且有配套固件，驱动 PCB 未就绪 | 验证真实 UART 握手、配置与状态回传，配合检查数字输出 | 串口日志与数字测量记录；不验收触觉 |
| 驱动 PCB、阵列和测量条件就绪 | 配合单点、位置／方向及图形实验 | 实测记录与参与者回答 |

第 3、5 节是供后续实现和联调的验收标准，现在只核对设计；第 6、7 节现在只准备文件与空白模板。当前 Demo 在电脑内模拟设备，不经过真实串口，也不能证明板端已实现。

板卡到货后的第一项验收是：**上位机发送完整配置，板端确认并回传真实已应用状态，开始／暂停／停止均能得到对应反馈。** 通信成功、数字波形正确、声场聚焦和人能辨识图形，应分别记录，不能互相替代。

## 2. 交接分工与交付物

| 事项 | 上位机负责人准备 | FPGA／硬件同学配合 |
|---|---|---|
| 协议对接 | 本清单、完整报文、预期回包、日志与复现步骤 | 实现解析、状态机、原子配置与快照 |
| 参数核对 | 确认界面与设备声明一致 | 确认 UART 电平／引脚、波特率、实际阵列与支持参数 |
| 实验准备 | 保存固定测试图形，整理记录表 | 确认驱动与输出条件，提供实际测量 |
| EXE 试用 | 提供便携 ZIP，收集操作问题 | 在另一台 Windows 64 位电脑上试用 |

当前 EXE 和 ZIP 位于工作区 `desktop_app/dist/`，没有上传为 GitHub Release。源码、构建与自检方法见[上位机使用说明](desktop_app/README.md)。源码变更后需要重新打包 EXE。

## 3. FPGA 最小对接清单

此节用于交给 FPGA 同学实现，上位机负责人现在核对约定即可。实物接线与板端行为验收等开发板和固件就绪后进行。

### 3.1 先确认接口，再实现基础闭环

- [ ] 约定两端波特率，默认 115200 baud、8N1、无流控；确认接线和 IO 电平。
- [ ] ASCII 按 LF 分帧，支持分片／连续多帧接收，CRC16 校验，最长 8192 字节；错误帧不得应用部分配置。
- [ ] 实现 `HELLO`、`PING`、`CONFIG`、`MODE`、`START`、`PAUSE`、`STOP`、`SNAP`。
- [ ] HELLO 返回实际接线阵列、容量、坐标范围、`boot` 和能力声明；不能把软件最大容量当作实际硬件规模。
- [ ] 当前客户端握手要求 `CONFIG,MODE,START,PAUSE,STOP,STATE,PHASE` 能力；所有 `max_*` 容量字段须为正数。未支持的图案／参数必须明确拒绝，不能默默替换。
- [ ] 首先对接固定点；在 `REMOTE+IDLE` 下完整验证 CONFIG 后一次生效，`rev` 加一，返回 ACK 和匹配的完整 STATE。只回 ACK 不会解锁播放。
- [ ] STATE 回传完整配置、实际阵列、状态、焦点坐标、扫描开关、段号和相位；相位数量等于实际通道数。锁存同一时刻的数据后再发送。
- [ ] 上电／复位关闭输出；暂停关闭输出但保留进度，停止关闭输出并归零。重连不会自动开始。
- [ ] 心跳与控制来源正确：REMOTE 超时停机；LOCAL 断开电脑允许板端独立运行；STOP 在两种模式均有效。

`boot` 每次复位改变；同一启动周期中 `sample` 递增，`uptime_ms`、`rev` 不倒退。真实设备 HELLO 与 STATE 使用 `simulated=0`；Demo 使用 1，不能通过修改来源标志伪装已完成实物验证。

### 3.2 草图上传的最低扩展

- [ ] 增加 `SCAN_PATHS` 能力，按真实容量声明 `max_scan_points`、`max_strokes`，上位机按声明限制发送。
- [ ] 接收完整有序路径，板内独立插值、计算相位和更新输出。CAD 几何关系由电脑处理，不交给 FPGA。
- [ ] 段间及末段返回首段时关闭输出，正确回传 `scan_on`、`stroke_index`；不能把分离轮廓连成有触觉的线。
- [ ] 容量、工作范围、阵列声明或跳转时间不满足要求时拒绝，保留上一份有效配置。

只做固定点可以先验证基础通信，但还不能宣称草图上传已对接完成。旧点列支持由 `CUSTOM_XY` 声明；不得声明未实现的路径能力。

### 3.3 回传时序

上位机约每 0.8 秒发 PING；命令应答超过 2 秒、状态超过 2.5 秒未到达会断开，超过 1.6 秒的状态不再作为实时反馈。控制 ACK 后还需在 3.5 秒内收到匹配 STATE。板端心跳期限需至少 2000 ms，当前 Demo 使用 3000 ms。

按实际帧长核算串口占用：8N1 的发送时间约为 `字节数 × 10 / 波特率`。例如 8192 字节在 115200 baud 下约需 0.711 秒，仅为整帧传输时间。真实串口不能照搬 Demo 的 50 ms 全量回传；需统筹相位数量、路径长度、快照周期与应答时限。当前客户端要求完整 STATE，若要改为增量反馈，必须两端一起修改协议。

## 4. 可复现通信样例

以下报文由当前 `encode()` 和 `DemoDevice` 生成并校验，采用 4×4、10 mm 间距。示例固定 `boot=handoff01`、时钟为 0，以便复现；示例回包是模拟数据，实际设备的时间、启动编号和状态不能照抄。

代码块中的每行是一帧，实际发送时在行尾追加 LF 字节。字段或序号变化后必须重新计算 CRC。人工逐条操作时也须持续发送 PING，不能因阅读间隔触发心跳超时。

### 4.1 握手

请求与 Demo ACK：

```text
HAP3 CMD 1 HELLO*F6AD
HAP3 ACK 1 HELLO proto=3 device=DEMO-FPGA boot=handoff01 simulated=1 hb_ms=3000 caps=CONFIG,MODE,START,PAUSE,STOP,STATE,PHASE,CUSTOM_XY,SCAN_PATHS max_rows=16 max_cols=16 max_channels=256 max_nodes=64 max_scan_points=256 max_strokes=32 hw_rows=4 hw_cols=4 hw_pitch_um=10000 mapping=ROW_MAJOR_XY x_min_um=-100000 x_max_um=100000 y_min_um=-100000 y_max_um=100000 z_min_um=20000 z_max_um=300000*AC20
```

ACK 后还会有一帧完整 STATE，初始状态应为 IDLE、输出关闭。HELLO 中的范围和容量只是此 Demo 的声明，实物按自身条件填写。

### 4.2 提交固定点

焦点为 `(0, 0, 150)` mm，`level=0`，用于先检查数字链路。150 mm 是测试坐标，不代表已验证的触觉距离。以下依次为请求、ACK 和完整 STATE：

```text
HAP3 CMD 2 CONFIG carrier_hz=40000 phase_steps=64 cx_um=0 cy_um=0 z_um=150000 radius_um=0 repeat_millihz=500 mod_hz=200 level=0 shape=POINT path_xy_um=NONE path_closed=1 scan_paths=NONE blank_us=2000 hw_rows=4 hw_cols=4 hw_pitch_um=10000 mapping=ROW_MAJOR_XY*1115
HAP3 ACK 2 CONFIG applied=1 rev=1*2976
HAP3 TEL 0 STATE boot=handoff01 sample=2 uptime_ms=0 rev=1 mode=REMOTE state=IDLE output=0 scan_on=1 stroke_index=0 simulated=1 reason=NONE carrier_hz=40000 phase_steps=64 cx_um=0 cy_um=0 z_um=150000 radius_um=0 repeat_millihz=500 mod_hz=200 level=0 shape=POINT path_xy_um=NONE path_closed=1 scan_paths=NONE blank_us=2000 hw_rows=4 hw_cols=4 hw_pitch_um=10000 mapping=ROW_MAJOR_XY fx_um=0 fy_um=0 fz_um=150000 phases=21,26,26,21,26,31,31,26,26,31,31,26,21,26,26,21*C667
```

核对 STATE 中 `rev=1`、配置与请求相同、16 个相位码齐全；此处相位来自参考模型。真实固件需回读已应用相位，不能用上位机预测值代替。

### 4.3 控制与回包

以下各对报文依次为请求与 ACK；除 PING 外，每次 ACK 后还应跟完整 STATE（此处省略重复长帧）：

```text
HAP3 CMD 3 PING*F56B
HAP3 ACK 3 PING applied=1 rev=1*8A9E
HAP3 CMD 4 START*CBCB
HAP3 ACK 4 START applied=1 rev=1*179B
HAP3 CMD 5 PAUSE*9418
HAP3 ACK 5 PAUSE applied=1 rev=1*1217
HAP3 CMD 6 START*AB28
HAP3 ACK 6 START applied=1 rev=1*DD8D
HAP3 CMD 7 STOP*38C3
HAP3 ACK 7 STOP applied=1 rev=1*D9F7
```

| 顺序 | 预期状态 | 核对事项 |
|---|---|---|
| PING | IDLE | 刷新心跳，不启动 |
| START | RUNNING | 配置版本保持 1 |
| PAUSE | PAUSED | 保留进度，关闭输出 |
| START | RUNNING | 继续运行 |
| STOP | IDLE | 关闭输出，进度归零 |

本样例 `level=0`，即使 RUNNING 也应 `output=0`。固定点不会移动，不能用这个样例检查动画位移；后续用直线样例检查。切换到可输出等级须由硬件负责人确认具体驱动条件。

### 4.4 阵列不匹配

实际 4×4，却在相同固定点 CONFIG 中声明 8×8，应拒绝且 `rev` 保持 1：

```text
HAP3 CMD 8 CONFIG carrier_hz=40000 phase_steps=64 cx_um=0 cy_um=0 z_um=150000 radius_um=0 repeat_millihz=500 mod_hz=200 level=0 shape=POINT path_xy_um=NONE path_closed=1 scan_paths=NONE blank_us=2000 hw_rows=8 hw_cols=8 hw_pitch_um=10000 mapping=ROW_MAJOR_XY*11D6
HAP3 ERR 8 CONFIG code=HARDWARE_MISMATCH*DCCA
```

大阵列可以运行小范围图形，但 CONFIG 中的 `hw_*` 必须填写当前实际阵列。图形尺寸与坐标不因阵元数改变。

## 5. 异常联调验收表

当前可用 Demo 或软件测试检查上位机反应；表中的真实板端行为留待硬件联调，不在无板阶段勾选为通过。

| 测试 | 做法 | 通过条件 |
|---|---|---|
| 错误 CRC | 改 CONFIG 一个数字但不重算 CRC | 不应用、不误启动，后续正确帧可恢复 |
| 分片／粘连 | 将一帧分多次发送，或多帧一起发送 | 每帧只执行一次，无半帧更新 |
| 重复字段／超长行 | 重复字段名，或超过 8192 字节 | 拒绝非法帧；超长行丢弃到 LF 后恢复 |
| 参数／容量超限 | 非法 level、超容量路径或越设备范围 | 返回 ERR，保留有效配置；路径不截断 |
| 运行中改配置 | START 后发送 CONFIG | 返回 BUSY，原运行内容保持 |
| LOCAL 控制 | IDLE 时切 LOCAL，再从电脑 START／CONFIG | 返回 LOCAL_CONTROL；STOP 仍可用 |
| 只回 ACK | CONFIG ACK 后不发匹配 STATE | 上位机不允许播放，并按时限报告失败 |
| 回传中断 | 停止 STATE；单独测试停止 PING | 上位机清除实时位置；REMOTE 板端心跳超时停机 |
| 复位／旧快照 | 改 boot，或发送旧 sample | 复位要求重连；旧快照不覆盖新状态 |

上位机可能提前阻止非法配置；要验证板端拒绝逻辑，应使用独立协议测试发送端或 RTL testbench。测试均保存请求、回包、固件版本和结果，不把 ERR 本身误判成通信失败。

## 6. 首轮图形与感知实验准备

先保存以下独立图形为 JSON，尺寸与偏移明确记录；这些是可调整的起始样例，不是已验证的触觉参数。

| 样例名 | 内容／初始坐标，单位 mm | 验证目标 |
|---|---|---|
| `P0-center.json` | 固定点 `(0, 0)` | 单点是否可重复感知 |
| `P1-left.json`、`P2-right.json` | 固定点 `(-10, 0)`、`(10, 0)`，分别呈现 | 是否能区分两个位置；不同时输出 |
| `L1-horizontal.json` | `(-10, 0)` 到 `(10, 0)` | 位置移动与方向感知 |
| `L2-vertical.json` | `(0, -10)` 到 `(0, 10)` | 与水平线对比 |
| `G1-two-strokes.json` | 两条分开的短线 | 数字层面验证跳转关闭输出，无额外连接线 |

上位机负责人准备与保存图形，检查导入后坐标和尺寸不变。高度、输出等级、调制与扫描参数由联调双方记录；Demo 的慢速动画参数不能直接当作触觉实验参数。

实际验证先确认电气输出与测量条件，再验证单点、位置／方向区分，最后尝试完整图形。4×4 用于小规模链路验证，8×8／16×16 的触觉效果均待测，不预填“能辨识”。不要为了演示完整而直接跳到圆、三角形。

## 7. 实验记录模板

每次只改变少量参数，保留原始日志和图形文件；文件名建议 `YYYY-MM-DD_样例名_试次`。

| 项目 | 填写内容 |
|---|---|
| 标识 | 日期、试次、操作者；参与者用匿名编号 |
| 软件与硬件 | EXE／源码版本、固件版本、实际阵列行列与间距 |
| 图形 | JSON 文件、尺寸、偏移、显示高度与实际放手距离 |
| 输出设置 | carrier_hz、phase_steps、level、mod_hz、repeat_millihz、blank_us |
| 实际数据 | 示波器／接收器记录、实测更新率；未测项目填“未测” |
| 感知结果 | 无感觉／不确定／可感知；实际回答的位置或方向 |
| 辨识统计 | 呈现顺序、正确次数／有效试次数，不预先告知本次答案 |
| 关联材料 | 配置、通信日志、测量文件和问题截图的相对路径 |

`level` 是软件等级，不是实测电压；数字相位回读不是声压。没有硬件时只记录 Demo／软件验证，不填写真实触感或辨识率。

## 8. 上位机负责人近期执行清单

### 8.1 现在可完成，不需要任何板子

- [ ] 与 FPGA 同学核对第 3 节，约定最先支持的固定点配置和路径容量；实际能力待实现后验收。
- [ ] 交付第 4 节报文；用当前编解码器和 Demo 检查序号、CRC、完整 STATE、心跳和错误处理，不要求连接 COM 端口。
- [ ] 实际保存第 6 节图形 JSON，并逐个导入检查；本文件当前提供规格，未附生成好的 JSON 包。
- [ ] 请队友在另一台未安装 Python 的 Windows 64 位电脑试用 ZIP：启动→绘图→保存→关闭→导入→连接 Demo→发送→播放→停止。
- [ ] 按“版本、操作步骤、预期、实际、截图／日志”收集问题，优先修复阻碍上述流程的缺陷。

### 8.2 硬件就绪后再执行

- [ ] FPGA 板与固件就绪：通过真实串口验证固定点通信、多段路径、状态回传与异常恢复。
- [ ] 驱动 PCB、阵列及测量条件就绪：配合数字／声场测量，再开展单点与方向感知实验。

本机 EXE 已完成独立目录运行与 Demo 自检；另一台电脑兼容性、真实串口链路和板卡输出仍是待验收项。

## 9. 依据与维护

- [协议字段、时序与状态语义](desktop_app/PROTOCOL.md)。
- [编解码与 CRC 实现](desktop_app/protocol.py)、[客户端握手与超时检查](desktop_app/controller.py)。
- [Demo 状态机](desktop_app/demo.py)、[坐标、路径与相位参考](desktop_app/model.py)。
- [EXE 构建、自检和界面使用](desktop_app/README.md)。

本文不新增声学性能承诺。协议改变时同步更新本文报文并重新生成 CRC；实际实现进度应另记结果与版本，不能直接把本清单勾选为已完成。
